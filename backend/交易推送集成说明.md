# 交易推送功能集成说明

## ✅ 已完成部分

### 1. 推送服务 (`app/services/notification/notification_service.py`)
- ✅ 创建通知服务类
- ✅ 支持多种通知类型 (开仓、平仓、止盈、止损、风险预警)
- ✅ 支持多种通知级别 (info, success, warning, error)
- ✅ WebSocket推送接口

### 2. WebSocket管理器扩展 (`app/websocket/manager.py`)
- ✅ 添加用户级别推送支持
- ✅ 用户ID和sid映射管理
- ✅ `send_personal_message()` 方法
- ✅ 用户认证事件 `authenticate`

### 3. 服务注入 (`app/main.py`)
- ✅ WebSocket管理器注入到通知服务
- ✅ 应用启动时初始化

## 🔧 需要集成的代码修改

### 修改1: 波段策略开仓推送

**位置**: `swing_long_strategy.py` 的 `_open_position()` 方法

**在成功开仓后添加:**

```python
# 位置: _open_position() 方法的最后
if order and order.get("ordId"):
    logger.info(f"✅ 开仓订单提交成功: {order.get('ordId')}")

    # 保存订单到数据库
    await self._save_order_to_db(...)

    # 更新持仓信息
    self.position = {...}

    # 🆕 发送开仓通知
    await notification_service.notify_position_opened(
        user_id=self.user_id,
        strategy_id=self.strategy_id,
        strategy_name=self.strategy_name or f"波段策略{self.strategy_id}",
        symbol=self.symbol,
        side="buy",
        entry_price=float(entry_price),
        amount=float(contract_amount),
        leverage=self.leverage,
        margin=float(margin)
    )
```

### 修改2: 波段策略平仓推送

**位置**: `swing_long_strategy.py` 的 `_close_position()` 方法

**在成功平仓后添加:**

```python
# 位置: _close_position() 方法的最后
if order and order.get("ordId"):
    logger.info(f"✅ 平仓订单提交成功: {order.get('ordId')}")

    # 保存订单到数据库
    await self._save_order_to_db(...)

    # 计算盈亏
    entry_price = self.position["entry_price"]
    coin_amount = self.position["amount"]
    pnl = (current_price - entry_price) * coin_amount * self.leverage
    pnl_pct = ((current_price - entry_price) / entry_price) * 100 * self.leverage

    # 🆕 发送平仓通知
    await notification_service.notify_position_closed(
        user_id=self.user_id,
        strategy_id=self.strategy_id,
        strategy_name=self.strategy_name or f"波段策略{self.strategy_id}",
        symbol=self.symbol,
        side="buy",
        entry_price=float(entry_price),
        exit_price=float(current_price),
        amount=float(coin_amount),
        pnl=float(pnl),
        pnl_pct=float(pnl_pct),
        reason=reason  # "止盈 15.5%" 或 "止损 -5.2%"
    )

    # 清空持仓
    self.position = None
    self.highest_price = Decimal("0")
```

### 修改3: 风险预警推送

**位置**: `swing_long_strategy.py` 的 `_monitor_position()` 方法

**在达到最大止损次数时添加:**

```python
# 位置: 止损后检查止损次数
if self.stop_loss_count >= self.max_stop_loss_count:
    logger.error(f"❌ 达到最大止损次数 {self.max_stop_loss_count},策略停止交易")

    # 🆕 发送风险预警
    await notification_service.notify_risk_warning(
        user_id=self.user_id,
        strategy_id=self.strategy_id,
        strategy_name=self.strategy_name or f"波段策略{self.strategy_id}",
        symbol=self.symbol,
        warning_type="max_stop_loss",
        message_text=f"达到最大止损次数({self.max_stop_loss_count}次),策略已停止",
        data={
            "stop_loss_count": self.stop_loss_count,
            "max_stop_loss_count": self.max_stop_loss_count
        }
    )
```

### 修改4: 策略总盈利推送

**位置**: 每次平仓后计算累计盈利

```python
# 在平仓后,从数据库查询累计盈利
from app.core.database import SessionLocal
from app.models.strategy import Strategy

db = SessionLocal()
strategy = db.query(Strategy).filter(Strategy.id == self.strategy_id).first()
if strategy:
    await notification_service.notify_profit_realized(
        user_id=self.user_id,
        strategy_id=self.strategy_id,
        strategy_name=strategy.name,
        total_profit=float(strategy.total_profit),
        total_trades=strategy.total_trades,
        win_rate=float(strategy.win_rate)
    )
db.close()
```

## 📱 前端WebSocket接收

### 前端连接和认证

```typescript
// 连接WebSocket
import { io } from 'socket.io-client'

const socket = io('http://localhost:8000', {
  transports: ['websocket', 'polling']
})

// 连接成功后认证
socket.on('connected', () => {
  // 发送用户认证
  socket.emit('authenticate', { user_id: 1 }) // 从用户store获取
})

socket.on('authenticated', (data) => {
  console.log('WebSocket认证成功:', data)
})
```

### 接收推送通知

```typescript
// 监听通知事件
socket.on('message', (message) => {
  if (message.event === 'notification') {
    const notification = message.data

    // 根据级别显示不同样式
    switch (notification.level) {
      case 'success':
        // 成功通知 (绿色)
        showNotification({
          type: 'success',
          title: notification.title,
          message: notification.message
        })
        break

      case 'warning':
        // 警告通知 (黄色)
        showNotification({
          type: 'warning',
          title: notification.title,
          message: notification.message
        })
        break

      case 'error':
        // 错误通知 (红色)
        showNotification({
          type: 'error',
          title: notification.title,
          message: notification.message
        })
        break
    }

    // 浏览器原生通知 (可选)
    if (Notification.permission === 'granted') {
      new Notification(notification.title, {
        body: notification.message,
        icon: '/logo.png'
      })
    }
  }
})
```

### 使用Ant Design通知组件

```typescript
import { notification } from 'antd'

socket.on('message', (message) => {
  if (message.event === 'notification') {
    const data = message.data

    // 使用Ant Design notification
    notification[data.level]({
      message: data.title,
      description: data.message,
      duration: 5, // 5秒后自动关闭
      placement: 'topRight'
    })
  }
})
```

## 🧪 测试推送功能

### 测试脚本

创建 `backend/test_notifications.py`:

```python
"""测试推送通知功能"""
import asyncio
from app.services.notification import notification_service

async def test_notifications():
    # 测试开仓通知
    await notification_service.notify_position_opened(
        user_id=1,
        strategy_id=1,
        strategy_name="BTC波段测试",
        symbol="BTC-USDT-SWAP",
        side="buy",
        entry_price=95000.0,
        amount=0.1,
        leverage=5,
        margin=1900.0
    )

    # 等待2秒
    await asyncio.sleep(2)

    # 测试平仓通知 (止盈)
    await notification_service.notify_position_closed(
        user_id=1,
        strategy_id=1,
        strategy_name="BTC波段测试",
        symbol="BTC-USDT-SWAP",
        side="buy",
        entry_price=95000.0,
        exit_price=97000.0,
        amount=0.1,
        pnl=100.0,
        pnl_pct=10.5,
        reason="止盈 10.5%"
    )

if __name__ == "__main__":
    asyncio.run(test_notifications())
```

## 📝 后续扩展

### 1. Telegram推送 (可选)

```python
# 在notification_service.py中实现
async def _send_telegram(self, user_id: int, message: Dict):
    """Telegram推送"""
    # 获取用户的Telegram chat_id
    # 调用Telegram Bot API发送消息
    import aiohttp

    bot_token = "YOUR_BOT_TOKEN"
    chat_id = get_user_telegram_chat_id(user_id)

    text = f"{message['title']}\n{message['message']}"

    url = f"https://api.telegram.org/bot{bot_token}/sendMessage"
    async with aiohttp.ClientSession() as session:
        await session.post(url, json={
            'chat_id': chat_id,
            'text': text
        })
```

### 2. 邮件推送 (可选)

```python
async def _send_email(self, user_id: int, message: Dict):
    """邮件推送"""
    # 使用SMTP发送邮件
    pass
```

### 3. 推送历史记录

创建 `notifications` 表存储推送历史,方便用户查看历史通知。

## 🎯 总结

推送系统已经完成框架搭建,还需要:

1. ✅ 推送服务实现
2. ✅ WebSocket管理器扩展
3. ✅ 服务注入
4. ⏳ 策略集成 (需要在开仓/平仓时调用推送)
5. ⏳ 前端WebSocket接收
6. ⏳ 前端通知UI
7. ⏳ 测试

按照本文档的说明,可以完成完整的交易推送功能! 🚀
